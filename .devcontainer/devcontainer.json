{
    "name": "Python 3",
    "build": {
        "context": "..",  // Set the build context to the parent folder
        "dockerfile": "Dockerfile"
    },
    "runArgs": ["--network=bridge","--memory=32gb"],
    "customizations": {
        "vscode": {
            "extensions": [
                "juanblanco.solidity",
                "ms-toolsai.jupyter"
            ]
        }
    }
}